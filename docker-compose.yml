version: '3.8'

services:
  streaming:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: streaming-app
    ports:
      - "8080:3000"
    restart: unless-stopped
    environment:
      - PORT=3000
    networks:
      - streaming-network

networks:
  streaming-network:
    driver: bridge

